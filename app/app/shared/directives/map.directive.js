angular.module('StoringenApp')
.directive('chartDirective', function() {
  'use strict';
  return {
    // template:  '<div google-chart agc-on-select="duplicate(type, chartData.data[selectedItem.row + 1][0])" chart="chartData"></div>'
  };
});
